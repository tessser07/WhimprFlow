# v2:bac89b58f54e4145d28b47c30994e8df2b49e79f92be28ac2248f791cbeb39ad

```json
{
  "gaps": [
    {
      "topic": "Cleanup-LLM model verification (local bundle + Claude toggle)",
      "prompt": "WhimprFlow is a local-first macOS dictation app (a from-scratch clone of Wispr Flow / wisprflow.ai) targeting an Apple M4 Pro with 24GB RAM on macOS 15.7. After local on-device speech recognition produces a raw transcript, a small LOCAL large language model (the 'cleanup model') edits that transcript: it removes filler words (um/uh), fixes punctuation, capitalization, and grammar, applies spoken self-corrections, and applies a user dictionary as a spelling authority. The task is framed as a conservative copy-edit (deletion + light normalization, NOT rewriting), so instruction-following and low latency matter far more than reasoning ability; the model must be small enough to stay resident in ~2.5-3GB alongside the ASR model and produce a ~50-word (~67-token) output in roughly under a second. The app also offers a toggle to use the Anthropic Claude API instead of the local model for higher-quality cleanup.\n\nA prior research pass produced recommendations that were flagged as possibly citing models beyond the knowledge cutoff (some names it surfaced, e.g. 'Qwen3.5', 'Gemma 4', 'Claude Fable 5', 'M5 chips', may be synthetic or future). Those recommendations were: DEFAULT local model = 'Qwen3-4B-Instruct-2507' at Q4_K_M (claimed ~2.4-2.6GB, IFEval 83.4, 256K context, Apache-2.0 license, a non-reasoning 'Instruct' variant); ALTERNATES = 'Llama-3.2-3B-Instruct' (~2.0GB Q4, custom Llama Community License), 'Gemma-3-4B-it' with int4 QAT (~2.6GB, claimed IFEval 90.2, custom Gemma license), 'Phi-4-mini-instruct' (3.8B, MIT), and 'SmolLM3-3B' (Apache-2.0). For the Claude toggle the recommendation was 'Claude Haiku 4.5'.\n\nYOUR TASK \u2014 verify against primary sources and correct as needed:\n(a) LOCAL MODELS: For each candidate, confirm whether it actually exists as a released checkpoint as of mid-2026, and report its exact Hugging Face model ID, true parameter count, the on-disk size of an available 4-bit quant (GGUF Q4_K_M and/or MLX 4-bit), its license (specifically confirm Apache-2.0 vs. a custom/community license, since this must be legally redistributable inside a bundled closed-source-capable app), and any published instruction-following benchmark (IFEval). Explicitly determine whether 'Qwen3-4B-Instruct-2507' is a real, released, non-reasoning instruct checkpoint. If it is NOT real, or its specs differ materially, identify the single best REAL instruction-following model in the ~3-4B range for a bundled local text-cleanup task, preferring a clean permissive license (Apache-2.0 or MIT).\n(b) CLAUDE API TOGGLE: Confirm the current correct low-latency, low-cost Claude model for a short (~50-word) text-cleanup call (the 'Haiku-class' tier). Report the exact API model ID string to pass, input and output token pricing (USD per million tokens), context window, whether streaming is supported, and typical time-to-first-token / latency characteristics. Do not assume 'Claude Haiku 4.5' is correct \u2014 verify the current model lineup.\n\nWHERE TO LOOK: Hugging Face model cards and their LICENSE files for Qwen/Qwen3-4B-Instruct-2507, meta-llama/Llama-3.2-3B-Instruct, google/gemma-3-4b-it, microsoft/Phi-4-mini-instruct, HuggingFaceTB/SmolLM3-3B (and search HF for the correct current IDs if these 404); llm-stats.com and the Open LLM / IFEval leaderboards for instruction-following scores; and docs.claude.com / docs.anthropic.com (models overview and pricing pages) for the Claude model IDs, pricing, and latency. Report exact IDs, sizes, licenses, and prices with source URLs; flag any candidate you cannot confirm exists."
    },
    {
      "topic": "Auto-learned dictionary end-to-end mechanism",
      "prompt": "WhimprFlow is a local-first macOS dictation app (a from-scratch clone of Wispr Flow / wisprflow.ai) for an Apple M4 Pro on macOS 15.7. Pipeline: hold the Fn key to record, local on-device speech recognition (ASR) produces a raw transcript, a small local LLM cleans it up, and the result is pasted into the frontmost app's focused text field. One of the four HEADLINE features is an 'auto-learned dictionary': when the user speaks a proper noun, name, brand, or technical term that the ASR mis-transcribes, and the user then manually corrects the spelling in the target app, WhimprFlow should silently learn that word so future dictations spell it correctly \u2014 with no manual action from the user.\n\nKNOWN FACTS about the real Wispr Flow's behavior (from its help docs and a reverse-engineering teardown of its macOS app): It auto-adds ONLY 'distinctive/specialized words and proper nouns' from user corrections, and EXPLICITLY FILTERS OUT common words (it does not add words like 'sprint', 'feature', 'deploy', 'roadmap'). Auto-learned words are shown with a sparkle icon in the dictionary UI. Each dictionary word can carry at most one misspelling-replacement rule (correct spelling + the specific wrong spelling the ASR produces). Users can also manually add words and 'star' them for higher recognition priority. Reverse-engineering revealed an 'EditedTextManager' component that reads the FULL contents of the target text field via the macOS Accessibility API (observed reading up to ~36,000 characters) after each dictation. Wispr's own engineers describe the personalization as an on-device policy that 'decides when a correction should generalize,' applied as CONTEXT to the cleanup LLM rather than as a hard find-and-replace, noting that 'LLMs are phenomenal at recall but very low precision' at token-by-token stylistic rules.\n\nCRITICAL CONSTRAINT: The two leading local ASR engines this clone would use \u2014 NVIDIA Parakeet TDT 0.6B (run natively via FluidAudio/CoreML on the Apple Neural Engine) and Whisper (via whisper.cpp) \u2014 both have WEAK custom-vocabulary biasing. Parakeet has no hotword/word-boost API and its model card warns that words not in its vocabulary are unlikely to be recognized. whisper.cpp only offers a soft-biasing 'initial_prompt' capped at 224 tokens (only the last 224 are used), which cannot reliably enforce a large custom dictionary. So the dictionary largely CANNOT be enforced at the ASR stage.\n\nYOUR TASK \u2014 produce an implementable specification answering three sub-problems:\n(1) DETECTION: How to reliably detect that a user corrected a previously-dictated word. What to capture at insertion time (the exact inserted string, its location), when and how to re-read the target field afterward (poll on a timer? re-read on the next dictation? on app-focus-change via NSWorkspace notifications?), and how to diff the originally-inserted text against the later field contents to isolate the specific corrected word while ignoring unrelated edits the user made.\n(2) FILTERING: A concrete algorithm to decide whether a corrected word is 'distinctive' enough to auto-add (proper noun / rare / technical term) versus a common word to ignore. Evaluate options: a frequency word-list threshold (which lists?), capitalization + out-of-vocabulary heuristics, or a lightweight LLM classification call.\n(3) APPLICATION: Given that Parakeet and whisper.cpp cannot be strongly biased at the ASR stage, specify how the learned word actually appears correctly next time. Detail the pattern of passing the dictionary to the cleanup LLM as a 'spelling authority' that phonetically maps the ASR's mis-hearing (e.g. 'Monvi') to the correct spelling (e.g. 'Manvi'), including: how many dictionary entries can realistically be injected into the LLM prompt within a sub-second latency budget, whether to pre-filter the dictionary to phonetically-plausible candidates before injection, and whether whisper's initial_prompt adds any incremental value on top of the LLM approach.\n\nWHERE TO LOOK: docs.wisprflow.ai \u2014 the 'dictionary', 'context-awareness', and 'variable/custom vocabulary' help articles. Open-source Wispr alternatives that implement custom-word correction: Handy (github.com/cjpais/Handy) \u2014 specifically its Rust file managing transcription, the apply_custom_words() fuzzy-correction function and its filler/text filter; and VoiceInk (github.com/Beingpax/VoiceInk) \u2014 its AIPrompts.swift cleanup prompt that uses custom vocabulary as a spelling authority to replace phonetically-close transcription mistakes, and its dictionary handling. whisper.cpp initial_prompt / hotwords discussions (github.com/ggml-org/whisper.cpp issues #1979 and discussion #348). The NVIDIA Parakeet TDT 0.6B Hugging Face model card's notes on out-of-vocabulary words. And literature on phonetic matching for post-ASR correction (Double Metaphone / Soundex / edit-distance approaches)."
    },
    {
      "topic": "Temporal behavior of transcription: streaming vs batch, esp. hands-free long sessions",
      "prompt": "WhimprFlow is a local-first macOS dictation app (a from-scratch clone of Wispr Flow / wisprflow.ai) for an Apple M4 Pro on macOS 15.7, with a floating pill/bar at the bottom of the screen. It has two dictation modes: (A) PUSH-TO-TALK \u2014 hold the Fn key, speak, release; on release the app runs local speech recognition plus an LLM cleanup pass and pastes the finished cleaned text into the frontmost app's focused text field. (B) HANDS-FREE / LOCKED mode \u2014 the user double-taps Fn (or presses a dedicated shortcut) to LOCK the recording so they can keep talking without holding the key, then clicks a checkmark (checkmark = confirm+paste) or a cross (cross = discard) in the floating bar to finish. Hands-free sessions can run up to 20 minutes, with an on-screen warning at 19 minutes.\n\nESTABLISHED FACTS: For push-to-talk, the real Wispr Flow inserts the cleaned text ALL AT ONCE on key-release; its measured cloud pipeline (ASR + LLM cleanup + network) is about 700ms end-to-end after release. While recording, the floating bar shows a live, audio-reactive waveform (moving white bars that go flat after silence). Locally on the target M4 Pro, batch recognition of a short push-to-talk clip is very fast (a native Parakeet CoreML path processes ~1 minute of audio in ~0.5s), and the local LLM cleanup of ~50 words takes roughly 0.6-0.9s, so a batch-on-release design is viable for short push-to-talk dictations.\n\nWHAT IS UNRESOLVED and blocks the clone's architecture: the TEMPORAL behavior of text production during a LONG hands-free session. Specifically it is NOT known whether Wispr Flow, in hands-free/locked mode, transcribes and inserts text INCREMENTALLY as the user speaks (streaming partial results into the target field or into the pill throughout the session) or instead buffers the ENTIRE multi-minute session and only runs recognition + cleanup + insertion once, when the user clicks the checkmark. This decision determines whether WhimprFlow must build a true STREAMING recognition path (e.g. FluidAudio's streaming Parakeet / end-of-utterance variants, or chunked recognition during the recording) versus a simple BATCH finalizer that processes the whole recording at the very end \u2014 and whether the LLM cleanup runs per-chunk or once over the full transcript. It is also unclear whether the floating bar ever displays live transcribed TEXT (a text preview) during recording, or only the waveform.\n\nYOUR TASK \u2014 determine, for Wispr Flow specifically and corroborated by how comparable shipping desktop dictation apps behave:\n(1) In hands-free / locked mode, is transcribed text inserted incrementally during the session, or only when the user stops (clicks the checkmark)? If incremental, at what granularity \u2014 per phrase, per pause, per sentence?\n(2) During push-to-talk (hold), does any live or partial transcription appear (in the pill or the target field) BEFORE release, or is nothing shown until release?\n(3) Does the floating bar ever render live transcribed TEXT (not just an audio waveform) during recording?\n(4) For a very long dictation (multiple minutes), does the user experience a single long processing delay at the end, or is it streamed/chunked so text keeps flowing?\n\nWHERE TO LOOK: docs.wisprflow.ai \u2014 the 'Use Flow hands-free', 'Starting your first dictation', and 'Navigating the app / move-and-dock' help articles. YouTube walkthroughs and written reviews that actually SHOW the on-screen behavior during a long hands-free session (e.g. podfeet.com's Wispr Flow review and other dictation-app review blogs). For corroboration of architectural norms among local/desktop dictation apps, examine how each handles streaming vs batch and any reported end-of-session latency for long recordings: superwhisper, MacWhisper, VoiceInk (github.com/Beingpax/VoiceInk \u2014 note whether its Whisper path is whole-buffer/batch), and Handy (github.com/cjpais/Handy \u2014 which exposes BOTH a streaming partial-result path start_stream() emitting live preview events AND a batch transcribe() path; determine which models/modes trigger each). Report clearly which behavior Wispr Flow uses in each mode and cite where each finding comes from."
    }
  ]
}
```
