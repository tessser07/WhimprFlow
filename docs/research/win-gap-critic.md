# v2-de130e525966d818754f4de4f2b8118cc79b3 (critic)

```json
{
  "gaps": [
    {
      "topic": "ASR engine decision: real Apple-Silicon latency/power for ONNX-Parakeet (ort CPU/CoreML EP) vs FluidAudio CoreML-ANE \u2014 does the single-engine one-codebase path actually meet the Mac budget, or must FluidAudio (Swift) return as a Mac-only backend?",
      "prompt": "WhimprFlow is a local-first, push-to-talk dictation app (an exact-behavior clone of Wispr Flow) being built for BOTH macOS (Apple Silicon, M1-M4, macOS 14+) and Windows 10/11 from ideally ONE codebase. The chosen stack is Tauri v2 with a Rust core, following the open-source dictation app 'Handy' (github.com/cjpais/Handy). On-device speech recognition uses NVIDIA's Parakeet TDT 0.6B transducer model (v2 English / v3 multilingual), batch-finalized on hotkey release (push-to-talk end).\n\nYou must resolve a single architecture-deciding question with the hardest numbers you can find.\n\nBACKGROUND: The original macOS-only design chose FluidAudio (a Swift/CoreML SDK, github.com/FluidInference/FluidAudio) to run Parakeet on the Apple Neural Engine (ANE), and cited a measured ~150-250 ms from key-release to raw transcript for a ~10 s utterance on an M4 Pro (~110-145x realtime / RTFx, 2.1% WER). FluidAudio is Swift-only and does NOT exist on Windows. To keep ONE codebase, the current plan is to DROP FluidAudio and run Parakeet as ONNX through the Rust `ort` crate (ONNX Runtime) via `transcribe-rs` (github.com/cjpais/transcribe-rs, what Handy ships) on BOTH platforms, selecting per-OS execution providers (CoreML on Mac, DirectML/CUDA/CPU on Windows).\n\nTHE PROBLEM: ONNX Runtime's CoreML execution provider reportedly does NOT support transducer/RNN operators, and Parakeet TDT is a FastConformer transducer. So on macOS the ONNX model may fall back largely to the CPU execution provider (NO ANE), not the Neural Engine. Crucially, NO published Apple-Silicon RTF/latency figure exists for ort/sherpa-onnx Parakeet int8 \u2014 every estimate in circulation (~300-550 ms on M4 Pro) is SCALED from an ARM Cortex-A76 single-board-computer benchmark, not measured on Apple Silicon.\n\nRESEARCH AND ANSWER:\n1. Does ONNX Runtime's CoreML EP support the operators in an exported Parakeet-TDT-0.6B ONNX graph (FastConformer encoder + transducer decoder + joiner)? Which subgraphs get partitioned to the CPU EP? Cite the current onnxruntime CoreML EP supported-op list and any relevant GitHub issues.\n2. What is the ACTUAL measured (or best-available) latency and RTF of Parakeet-TDT-0.6B int8 via `ort`/`transcribe-rs`/sherpa-onnx on Apple Silicon (any of M1-M4), for both CPU EP and CoreML EP, for a ~10 s utterance, batch finalize? Search Handy issues/discussions, sherpa-onnx issues, and any blog benchmark. If none exists, give a defensible estimate WITH method and confidence, and say plainly that it is unmeasured.\n3. How does that compare to FluidAudio's native CoreML/ANE Parakeet on the same machine class, on BOTH (a) latency and (b) sustained power draw / thermals / battery \u2014 the ANE is dramatically lower-power than running a conformer on the CPU, which matters for an always-resident dictation app.\n4. BOTTOM LINE: Does the single-engine ort/Parakeet path plausibly meet a perceived ~300-500 ms / committed <900 ms budget on a mid-tier Apple-Silicon laptop? Or must WhimprFlow keep FluidAudio as a Mac-only ASR backend behind an engine-abstraction trait from v1 \u2014 which reintroduces the Swift dependency the whole Tauri+Rust pivot exists to eliminate, and forks the 'one codebase' promise? State which, and give the specific numbers that decide it.\n\nReference points to investigate: Handy and its `transcribe-rs` crate; sherpa-onnx (k2-fsa) execution-provider handling; the `ort` crate (pyke) CoreML EP docs; FluidAudio's Documentation/Benchmarks.md; the nvidia/parakeet-tdt-0.6b-v2 Hugging Face model card. Mark every claim OBSERVED (measured/cited primary source) or INFERRED (estimate), and never present a scaled estimate as if it were measured on Apple Silicon."
    },
    {
      "topic": "Tauri vs Electron: is Tauri v2's transparent, non-activating, click-through, always-on-top pill overlay actually artifact-free on real Windows 11 hardware, or do reported transparency bugs make Electron (Wispr's own choice) the safer shell?",
      "prompt": "WhimprFlow is a local-first, push-to-talk dictation app (an exact-behavior clone of Wispr Flow) being built for BOTH Windows 10/11 and macOS from ideally ONE codebase. Its signature UI is a small floating 'pill' overlay: bottom-center of the screen, always on top, semi-transparent with rounded corners and a live audio waveform, that does NOT steal keyboard focus from the app the user is dictating into (so injected text lands in the right place), and is click-through everywhere except over its 1-2 small interactive buttons (cancel / stop).\n\nThe proposed stack is Tauri v2 (Rust core + system WebView2 on Windows), chosen over Electron. The ENTIRE justification for picking Tauri over Electron rests on one claim: the open-source dictation app 'Handy' (github.com/cjpais/Handy) already ships this exact overlay on Windows using Tauri, via a WebviewWindow with `transparent:true`, `decorations:false`, `always_on_top:true`, `skip_taskbar:true`, `focusable:false`, `shadow:false`, `accept_first_mouse:true`, plus a Win32 `SetWindowPos(HWND_TOPMOST, SWP_NOMOVE|SWP_NOSIZE|SWP_NOACTIVATE)` re-assert on a timer, and `setIgnoreCursorEvents()` toggled for click-through. Electron is the fallback: Wispr Flow itself ships Electron on Windows (paying ~800 MB RAM), so Electron is the proven-in-production alternative.\n\nTHE RISK YOU MUST RESOLVE: several reported Tauri issues threaten this overlay on real Windows 11 hardware, and NONE have been verified on-device for a production floating-pill product:\n- Tauri #8308 ('V2 window.transparent not work') and reports of a Chromium/WebView2 black or opaque rectangle on certain GPUs when transparency is toggled at runtime rather than set at build time.\n- Tauri #7328 (transparency + click-through + above-fullscreen artifacts on Windows 11).\n- Tauri has NO per-region hit-testing for click-through (tauri-apps/tauri #13070): the whole window is either click-through or not. Handy works around this by sizing the window tight to the visible pill and/or polling the cursor position to toggle `setIgnoreCursorEvents`.\n- An always-on-top overlay cannot appear over TRUE exclusive-fullscreen DirectX apps on Windows (the DWM compositor is bypassed) \u2014 determine whether this limitation is identical for Electron (a wash) or whether one stack handles borderless/flip-model fullscreen better.\n\nRESEARCH AND ANSWER, from primary sources (Tauri GitHub issues, Microsoft WebView2 docs, Handy source and issues, community reports) plus reasoning:\n1. On Windows 10/11 with common GPUs (Intel iGPU, NVIDIA, AMD), does Tauri v2's `transparent:true` WebView2 window render per-pixel alpha (rounded, semi-transparent pill over arbitrary content) RELIABLY, or are the black-box / opaque-rectangle artifacts real and frequent? What are the exact mitigations (build-time transparency flag, fully-transparent backgroundColor, minimum WebView2 runtime version) and do they FULLY resolve it, or only sometimes?\n2. Does `focusable:false` on Windows produce a genuinely non-activating window (WS_EX_NOACTIVATE-class) that does NOT pull keyboard focus off the target app, while STILL letting the pill's own buttons receive clicks (via `accept_first_mouse`)? Confirm the focus behavior, not just the flag mapping.\n3. Is the click-through approach (window-level `setIgnoreCursorEvents` + tight-fit window and/or cursor polling, with no per-region hit-testing) robust enough for a resting pill with 1-2 hover buttons, or is it fragile in practice?\n4. VERDICT: Is Tauri v2 a safe choice for this Windows overlay, or do the artifacts/limitations make Electron (proven by Wispr itself) the lower-risk pick for a product whose core UI IS this pill? Be explicit about exactly which finding would flip the decision to Electron.\n\nMark each claim OBSERVED (cited issue/doc/source) or INFERRED. Where a claim can only be settled by building and running an actual Tauri app on real Windows 11 hardware, say so explicitly and describe the minimal reproduction test."
    },
    {
      "topic": "Cross-track conflict: in-process Rust FFI (Handy model, recommended) vs a separate native sidecar helper process (Wispr's actual shipped architecture) for the fidelity-critical native subsystems \u2014 the two proven references disagree.",
      "prompt": "WhimprFlow is a local-first, push-to-talk dictation app (an exact-behavior clone of Wispr Flow) for BOTH Windows 10/11 and macOS 14+ from ideally ONE codebase. Planned stack: Tauri v2 with a Rust core, following the open-source dictation app 'Handy' (github.com/cjpais/Handy). The app needs several 'fidelity-critical' native OS capabilities on each platform, and you must resolve WHERE that native code should live.\n\nThe capabilities:\n- A global low-level keyboard hook for hold-to-talk push-to-talk on a MODIFIER-ONLY trigger. On macOS the DEFAULT trigger is the bare Fn/Globe key (keycode 63 / maskSecondaryFn) and the app MUST SUPPRESS the OS's built-in Fn action (emoji/dictation). On Windows the default is Ctrl+Win. This requires an event-CONSUMING hook (macOS CGEventTap `.defaultTap`; Windows `WH_KEYBOARD_LL`), not a read-only listener.\n- Hook/tap HEALTH management. macOS CGEventTaps get disabled on `kCGEventTapDisabledByTimeout` / UserInput and must be re-enabled by a ~5 s health watchdog, plus a stale-held-key watchdog (a real Wispr bug once left keys stuck and suppressed ~145 spacebars). On Windows, if the WH_KEYBOARD_LL callback ever exceeds ~1000 ms, the OS SILENTLY REMOVES the hook with NO notification \u2014 the app just stops responding to the hotkey.\n- Text injection at the cursor (clipboard snapshot / set / paste-simulate / restore, guarded by clipboard change-count; Unicode-typing fallback), and secure-input detection (macOS `IsSecureEventInputEnabled`; Windows password-field / UIPI / elevated-target checks).\n- Accessibility / context reading (macOS AXUIElement; Windows UI Automation).\n\nTHE UNRESOLVED ARCHITECTURE CONFLICT: two PROVEN reference implementations disagree on where this native code lives, and the choice cascades through the whole native surface, IPC design, and crash-handling.\n- 'Handy' (Tauri/Rust, the stack being copied) does it ALL IN-PROCESS in the Rust core: crates `rdev` / `handy-keys` for the hook (rdev's macOS path uses a CGEventTap under the hood; its event-consuming `grab()` is reportedly 'historically shaky' on macOS and does NOT expose a tap-health/timeout watchdog), `enigo` for injection, and objc2 / core-graphics / the `windows` crate for FFI where those crates fall short.\n- Wispr Flow ITSELF (the app being cloned) does the OPPOSITE. Teardowns of BOTH its shipped builds show it isolates ALL of these subsystems in a SEPARATE native HELPER PROCESS: on macOS a Swift LSUIElement helper (bundle id `com.electron.wispr-flow.accessibility-mac-app`); on Windows a C#/.NET NativeAOT single-file `Wispr Flow Helper.exe` using SetWindowsHookExW + UI Automation + SendInput/clipboard. The helper talks to the app shell over length-prefixed JSON IPC and its message schema includes `TapState` / `TapWatchdog` / `TapRecoveryEvent`, `CheckStaleKeys` / `StaleKeysResponse`, `PasteText`, `SecureInputStatus`, and `WindowsKeyUpSimulation`. Wispr's shell is Electron, but the helper split is about ISOLATION, not language.\n- The macOS-only predecessor design left this explicitly open: 'Whether a separate privileged helper process is truly unnecessary ... Wispr split it into a helper for a reason on Electron; validate we don't need the split.'\n\nA compounding factor: WhimprFlow ALSO runs in-process, DURING a dictation, an ONNX ASR model and a local llama.cpp LLM (Qwen3-class) that can SATURATE CPU/GPU. If those share a process (and potentially a thread pool) with the keyboard hook, a stall could trip exactly the Windows ~1000 ms hook-removal / macOS tap-timeout failures above.\n\nRESEARCH AND ANSWER:\n1. What does Handy ACTUALLY do on each platform (read source under `src-tauri/src/shortcut/` and `src-tauri/src/`): does `handy-keys` on Windows use WH_KEYBOARD_LL, does it SUPPRESS keys, does it recover from the hook-timeout removal? Does `rdev::grab()` on macOS reliably suppress bare-Fn AND recover from `kCGEventTapDisabledByTimeout`, and does anything provide the tap-health/stale-key watchdog the design requires?\n2. Is running an event-CONSUMING keyboard hook with a robust health/timeout watchdog reliably feasible IN-PROCESS inside a Tauri/Rust app that also runs the WebView UI plus in-process ASR + LLM inference \u2014 or does co-locating heavy compute with the hook thread materially raise the risk of the Windows silent-hook-removal and macOS tap-timeout failures? Is a dedicated OS thread + runloop inside the same process sufficient, or not?\n3. Enumerate the concrete trade-offs of a separate native sidecar helper process (crash isolation; hook thread never blocked by ASR/LLM; dedicated runloop; easy full tap reinstall; but IPC latency, a second signed/notarized native binary per OS, more build/release complexity) versus all-in-process.\n4. Give a CLEAR recommendation for v1: in-process Rust FFI vs. a native sidecar helper, per platform, with the single most important deciding reason. If the answer differs between macOS and Windows, say so. Note whether the presence of in-process ASR+LLM tips the decision toward Wispr's sidecar model or whether Handy's in-process-with-dedicated-thread model is demonstrably safe.\n\nCite primary sources where possible: Handy source; the rdev / handy-keys / enigo crate docs and source; Microsoft Learn on WH_KEYBOARD_LL, LowLevelKeyboardProc, and the LowLevelHooksTimeout registry value; Apple's CGEventTap / kCGEventTapDisabledByTimeout docs. Mark each claim OBSERVED vs INFERRED."
    }
  ]
}
```
